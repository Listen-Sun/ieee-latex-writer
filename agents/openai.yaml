interface:
  display_name: "IEEE LaTeX Writer Open"
  short_description: "Draft, anonymize, revise, and audit IEEE LaTeX papers."
  default_prompt: "Use $ieee-latex-writer to draft, anonymize, revise, or audit an IEEE-style LaTeX paper."
